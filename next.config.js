/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['mediaformasi.com'], // Ganti dengan nama domain yang sesuai
  },
}

module.exports = nextConfig
